/* Add border around each skill cell */
td > div {
  border: 1px solid #ccc;
  padding: 4px;
  border-radius: 4px;
  font-size: 14px;
  line-height: 1.4;
}

/* Highlight upticks in green */
td > div > div {
  padding: 2px 4px;
}

td > div > div:nth-child(2), /* During */
td > div > div:nth-child(3)  /* After */ {
  transition: background-color 0.3s ease;
}

/* Style for green highlight applied via ngStyle/ngClass */
td > div > div[style*="lightgreen"] {
  background-color: #d0f5d0 !important;
  font-weight: bold;
}

/* Legend styles */
.legend {
  margin-top: 1rem;
  font-size: 14px;
}

.legend ul {
  list-style: none;
  padding-left: 0;
  margin: 0;
}

.legend li {
  margin: 6px 0;
}

.highlight-box {
  display: inline-block;
  width: 12px;
  height: 12px;
  background-color: #d0f5d0;
  border: 1px solid #999;
  margin-right: 8px;
  vertical-align: middle;
}

/* Force table and paginator to show properly */
::ng-deep .p-datatable {
  width: 100% !important;
}

::ng-deep .p-paginator {
  margin-top: 1rem;
  min-height: 40px;
}

/* Optional: Make charts scrollable on smaller screens */
highcharts-chart {
  max-width: 100%;
  overflow-x: auto;
  display: block;
}
